generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TACUPOINT {
  ACPID   Int     @id @default(autoincrement())
  ACUCODE String? @db.VarChar(10)
  ACUPART Int?    @default(0)
  ACUNAME String? @db.VarChar(20)
}

model TAPPROVALNUMBER {
  PID     Int     @id @default(autoincrement()) @db.SmallInt
  VERSION String? @db.VarChar(4)
  APPNO   String? @db.VarChar(50)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TCARINSDIAGNOSIS {
  ID        Int    @id @default(autoincrement())
  DIGCODE   String @db.VarChar(5)
  DIGTYPE   String @db.VarChar(20)
  DIGCLASS  String @db.VarChar(20)
  DIGDETAIL String @db.VarChar(20)

  @@index([ID], map: "ID")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TCARINSMEDCLASSIFICATION {
  ID      Int    @id @default(autoincrement())
  MEDCODE String @db.VarChar(5)
  CLSCODE String @db.VarChar(2)
  CLSNAME String @db.VarChar(20)
  SUBCODE String @db.VarChar(2)
  SUBNAME String @db.VarChar(20)

  @@index([ID], map: "ID")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TCARINSMEDCOMPONENT {
  ID       Int    @id @default(autoincrement())
  COMPCODE String @db.VarChar(20)
  COMPNAME String @db.VarChar(10)
  COMPTYPE String @db.VarChar(4)

  @@index([ID], map: "ID")
}

model TCHUNADISEASE {
  CDID    BigInt  @id @default(autoincrement())
  KCDCODE String? @db.VarChar(10)
  CPYRATE Int?    @default(0) @db.TinyInt
}

model TCOMPANY {
  COMPANYID Int     @id @default(autoincrement())
  GROUPCODE Int?    @default(0)
  GROUPNAME String? @default("") @db.VarChar(50)
  COMCODE   String? @default("0") @db.VarChar(8)
  COMNAME   String? @default("") @db.VarChar(50)
  NOTE      String? @db.VarChar(500)
  CRTIME    String? @default("") @db.VarChar(14)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TDRUGMASTER {
  ITEMID       BigInt   @id @default(autoincrement())
  ITEMCODE     String?  @db.VarChar(10)
  INSTYPE      String?  @db.VarChar(10)
  APPDATE      String?  @db.VarChar(8)
  ITEMTYPE     String?  @db.VarChar(20)
  ITEMTITLE    String?  @db.Text
  UNIT         String?  @default("") @db.VarChar(8)
  EXCODE       String?  @default("") @db.VarChar(4)
  MANUFAC      String   @default("") @db.VarChar(40)
  MAINCOM      String   @default("") @db.VarChar(10)
  DOSAGE       String   @default("") @db.VarChar(40)
  DOSE         Decimal? @db.Decimal(10, 4)
  ITEMCATEGORY String?  @db.VarChar(11)
  ITEMTITLEEN  String?  @db.VarChar(200)
  PRESCOND     String?  @db.VarChar(100)
  USAGE        Int?     @db.SmallInt
  PROMOTE      BigInt?
  ATCCODE      String?  @default("") @db.VarChar(10)
  ATCNAME      String?  @default("") @db.VarChar(200)
  NARC         String   @default("") @db.VarChar(16)

  @@index([ITEMCODE], map: "ITEMCODE")
}

model TDRUGPRICE {
  ITEMID    BigInt   @id @default(autoincrement())
  ITEMCODE  String?  @db.VarChar(10)
  INSTYPE   String?  @db.VarChar(20)
  APPDATE   String?  @db.VarChar(8)
  EXCODE    String?  @db.VarChar(4)
  UNITPRICE Decimal? @db.Decimal(9, 2)
  PRESCOND  String?  @db.VarChar(100)
  USAGE     Int?     @db.SmallInt
  PROMOTE   BigInt?
  NARC      String   @default("") @db.VarChar(16)
  INS30     String?  @default("N") @db.Char(1)
  INS50     String?  @default("N") @db.Char(1)
  INS80     String?  @default("N") @db.Char(1)

  @@index([APPDATE], map: "APPDATE")
  @@index([ITEMCODE], map: "ITEMCODE")
}

model TEXCELFORM {
  FORMID     Int     @id @default(autoincrement())
  EXCEL_FILE Bytes?  @db.Blob
  DESCR      String? @db.VarChar(128)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TEXCELTEMPLATE {
  TID       Int    @id @default(autoincrement())
  TCODE     String @default("0") @db.VarChar(10)
  SPTITLE   String @default("") @db.VarChar(64)
  EXCELFORM Bytes? @db.Blob
}

model THOLIDAY {
  DAYID   BigInt  @id @default(autoincrement())
  ANNUM   Int     @db.SmallInt
  HOLIDAY String? @db.VarChar(4)
  DESCR   String? @db.VarChar(20)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TINSMASTER {
  ITEMID       BigInt  @id @default(autoincrement())
  ITEMCODE     String? @db.VarChar(10)
  APPDATE      String? @db.VarChar(8)
  ITEMTYPE     String? @db.VarChar(20)
  ITEMTITLE    String? @db.Text
  SUBITEMTITLE String  @db.Text
  ITEMTITLEEN  String? @db.VarChar(200)
  ITEMCATEGORY String? @db.VarChar(20)
  ADDITIONAL   String? @db.VarChar(200)
  ADDOP        Int?    @db.TinyInt
  OPERATION    Int?    @db.TinyInt
  PROCEDURE    Int?    @default(0) @db.TinyInt
  OPDATE       Int?    @default(0) @db.TinyInt
  CHAPTER      String? @db.VarChar(10)
  BASECODE     Int     @default(0)

  @@index([ITEMCODE], map: "ITEMCODE")
  @@index([ITEMTYPE], map: "ITEMTYPE")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TINSMATERIAL {
  MATID     Int     @id @default(autoincrement())
  MATERIAL  String? @default("") @db.VarChar(20)
  VENDER    String? @default("") @db.VarChar(40)
  UNIT      Float?  @db.Float
  UNITPRICE Float?  @db.Float
  DATEST    String? @db.VarChar(8)
  DATEEN    String? @db.VarChar(8)
  MATCODE   String? @db.VarChar(12)
  CRTIME    String? @db.VarChar(12)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TINSPRICE {
  PRICEID   BigInt   @id @default(autoincrement())
  ITEMCODE  String?  @db.VarChar(10)
  APPDATE   String?  @default("") @db.VarChar(8)
  UNITPRICE Decimal? @default(0.00) @db.Decimal(12, 2)
  HOSPRICE  Decimal? @default(0.00) @db.Decimal(12, 2)
  DENPRICE  Decimal? @default(0.00) @db.Decimal(12, 2)
  ORGPRICE  Decimal? @default(0.00) @db.Decimal(12, 2)
  MAPRICE   Decimal? @default(0.00) @db.Decimal(12, 2)
  KMPRICE   Decimal? @default(0.00) @db.Decimal(12, 2)
  CAREPRICE Decimal? @default(0.00) @db.Decimal(12, 2)
  OMPRICE   Decimal? @default(0.00) @db.Decimal(12, 2)
  POINT     Decimal? @default(0.0000) @db.Decimal(10, 4)
  INS50     String?  @default("N") @db.Char(1)
  INS80     String?  @default("N") @db.Char(1)
  INS90     String?  @default("N") @db.Char(1)
  INS100    String?  @default("N") @db.Char(1)
  DUP       String?  @default("N") @db.Char(1)
  INSTYPE   String?  @default("") @db.VarChar(20)
  ADDOP     Int?     @default(0) @db.TinyInt

  @@index([APPDATE], map: "INXAPPDATE")
  @@index([ITEMCODE], map: "ITEMCODE")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TITEMCATEGORY {
  CATEGORYID    Int     @id @default(autoincrement())
  MAJORCATEGORY String? @default("") @db.VarChar(20)
  SUBCATEGORY   String? @default("") @db.VarChar(20)
  MINORCATEGORY String? @default("") @db.VarChar(20)
  CATNO         String? @default("") @db.VarChar(8)
  PACAT         Int?    @db.TinyInt
  ACTING        Int?    @db.TinyInt

  @@index([CATNO], map: "CATNO")
  @@index([PACAT], map: "PACAT")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TKCD7 {
  KCDID      BigInt           @id @default(autoincrement())
  KCDCODE    String?          @db.VarChar(10)
  KCDNAME_KR String?          @db.Text
  KCDNAME_EN String?          @db.Text
  INFECT     String?          @db.VarChar(10)
  GENDER     String?          @db.VarChar(2)
  AGE1       Int?             @db.SmallInt
  AGE2       Int?             @db.SmallInt
  INCOMLETE  TKCD7_INCOMLETE? @default(N)
  MT028      Float?           @default(0)
  DTYPE      String?          @db.VarChar(20)
  CHUNACHECK Int?             @default(0) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TKCD8 {
  KCDID       BigInt  @id @default(autoincrement())
  KCDCODE     String? @db.VarChar(10)
  KCDNAME_KR  String? @db.Text
  KCDNAME_EN  String? @db.Text
  INFECT      String? @db.VarChar(10)
  GENDER      String? @db.VarChar(2)
  AGE1        Int?    @db.SmallInt
  AGE2        Int?    @db.SmallInt
  INCOMLETE   Int?    @default(0) @db.TinyInt
  MT028       Float?  @default(0)
  DTYPE       String? @db.VarChar(20)
  MAINDISEASE String? @default("") @db.VarChar(2)
  CHUNACHECK  Int?    @default(0) @db.TinyInt

  @@map("TKCD8")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TKCDGROUP {
  TCODE     String @id @db.VarChar(7)
  GROUPNAME String @db.VarChar(75)
  DEPTH     Int
  KCD       String @db.VarChar(47)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TKMINSMEDICINE {
  MEDID     Int     @id @default(autoincrement())
  ITEMCODE  String  @db.Char(12)
  ITEMNAME  String? @default("") @db.VarChar(100)
  MANUFACT  String? @default("") @db.VarChar(100)
  UNITPRICE Float?  @default(0) @db.Float
  UNITWT    Float?  @default(0) @db.Float
  WEIGHT    Float?  @default(0) @db.Float
  TOTALWT   Float?  @default(0) @db.Float
  APPDATE   String? @default("") @db.VarChar(8)
  EXPDATE   String? @default("") @db.VarChar(8)
  UNIT      String? @default("g") @db.VarChar(4)
  OLDCODE   String? @db.VarChar(8)
}

model TKMINSMEDICINECOMPONENT {
  COMPONENTID Int     @id @default(autoincrement())
  COMPONENT   String? @default("") @db.VarChar(20)
  MANUFACT    String? @default("") @db.VarChar(40)
  UNITG       Float?  @db.Float
  RAWWT       Float?  @db.Float
  UNITPRICE   Float?  @db.Float
  APPDATE     String? @db.VarChar(8)
  APPEND      String? @db.VarChar(8)
  INSCODE     String? @db.VarChar(12)
  TOTALWT     Float?  @default(0) @db.Float
  OLDCODE     String? @db.VarChar(9)
  UNIT        String? @default("g") @db.VarChar(2)
  COMPNAME    String? @db.VarChar(8)
}

model TKMINSPRESC {
  PREID    Int     @id @default(autoincrement())
  OLDCODE  String? @db.VarChar(12)
  MEDNAME  String? @db.VarChar(40)
  COMPCODE String? @db.VarChar(12)
  COMPNAME String? @db.VarChar(40)
  COMPWT   Float?  @db.Float
}

model TLOCALGOV {
  GOVCODE  String @id @db.VarChar(12)
  LOCALGOV String @default("") @db.VarChar(100)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TMATERIALMASTER {
  MATID        BigInt   @id @default(autoincrement())
  ITEMCODE     String?  @db.VarChar(10)
  APPDATE      String?  @db.VarChar(8)
  ITEMTITLE    String?  @db.VarChar(255)
  ITEMSPEC     String?  @db.VarChar(20)
  UNIT         String?  @db.VarChar(20)
  MANUFACT     String   @db.VarChar(50)
  UNITPRICE    Decimal? @default(0.00) @db.Decimal(12, 2)
  ITEMCATEGORY String?  @db.VarChar(10)
  MATERIAL     String?  @db.VarChar(100)
  DUP          String?  @default("N") @db.VarChar(10)
  DEALER       String?  @db.VarChar(20)
  ADDITIONAL   String   @default("0") @db.VarChar(100)
  INSTYPE      String   @default("") @db.VarChar(20)

  @@index([ITEMCODE], map: "ITEMCODE")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TMEDICOMMASTER {
  MEDICOMID Int    @id @default(autoincrement())
  MATERIAL  String @default("") @db.VarChar(20)
  COMPANY   String @default("") @db.VarChar(40)
  PNAME     String @default("") @db.VarChar(40)
  MCODE     String @default("") @db.VarChar(10)
  RCODE     String @default("") @db.VarChar(15)
  SCODE     String @default("") @db.VarChar(15)
  PCODE     String @default("") @db.VarChar(10)
  APPDATE   String @default("") @db.VarChar(10)
  ENDDATE   String @default("") @db.VarChar(10)

  @@index([APPDATE], map: "APPDATE")
  @@index([COMPANY], map: "COMPNAME")
  @@index([ENDDATE], map: "ENDDATE")
  @@index([MATERIAL], map: "MATERIAL")
  @@index([MCODE], map: "MCODE")
  @@index([PCODE], map: "PRDTCODE")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TMEDICOMPURCHASE {
  PURCHASEID Int    @id @default(autoincrement())
  MCODE      String @db.VarChar(10)
  HERBCODE   String @db.VarChar(10)
  HERBNAME   String @default("") @db.VarChar(50)
  ASNAME     String @default("") @db.VarChar(50)
  COMPANY    String @default("") @db.VarChar(50)
  DATE       String @db.Char(8)
  QTY        Int    @default(0)
  PRICE      Int    @default(0)
  ORIGIN     String @default("") @db.VarChar(50)
  DTYPE      String @db.Char(1)

  @@index([DTYPE, HERBCODE, DATE], map: "IDX_TMEDICOMPURCHASE_DTYPE_HERBCODE_DATE")
  @@index([DTYPE, MCODE, DATE], map: "IDX_TMEDICOMPURCHASE_DTYPE_MCODE_DATE")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TNIMSDRUGMASTER {
  PRDUCT_CD String  @id @db.VarChar(20)
  PRDUCT_NM String? @db.VarChar(300)
  TYPE      String? @db.VarChar(4)
  EDI_CD    String? @db.VarChar(20)
  CRTIME    String? @db.Char(14)
  MODTIME   String? @db.Char(14)
  DISCD     Int?    @default(0) @db.TinyInt

  @@index([EDI_CD], map: "EDI_CD")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TNONCOVEREDITEM {
  ITEMID   Int     @id @default(autoincrement())
  ITEMNO   String? @default("") @db.VarChar(4)
  ITEMTYPE String? @default("") @db.VarChar(20)
  YEARYN   String? @default("") @db.VarChar(2)
  AREACODE Int?    @default(0) @db.TinyInt
  OCCRTYPE String? @default("") @db.VarChar(10)
  CODETYPE String? @default("") @db.VarChar(2)
  OPTYPE   String? @default("") @db.VarChar(8)
  REPTAREA String? @default("") @db.VarChar(10)
  ITEMCODE String? @default("") @db.VarChar(10)
  CAT1     String? @default("") @db.VarChar(80)
  CAT2     String? @default("") @db.VarChar(80)
  CAT3     String? @default("") @db.VarChar(80)
  ITEMNAME String? @default("") @db.VarChar(100)
  ITEMMEMO String? @default("") @db.VarChar(100)
  USED     String? @default("") @db.VarChar(3)
  APPDATE  String? @default("") @db.VarChar(8)
  ENDDATE  String? @default("") @db.VarChar(8)

  @@index([APPDATE], map: "APPDATE")
  @@index([ENDDATE], map: "ENDDATE")
  @@index([ITEMCODE], map: "ITEMCODE")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TPOPUPWINDOW {
  PWID      Int     @id @default(autoincrement())
  WIDTH     Int     @default(0)
  HEIGHT    Int     @default(0)
  TITLE     String? @default("") @db.VarChar(128)
  DOCUMENT  String? @default("") @db.VarChar(256)
  DUPLICATE Int?    @default(0) @db.SmallInt
  DISCD     Int?    @db.UnsignedTinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TPRESCCOMPONENTMASTER {
  PRESCCOMID Int    @id @default(autoincrement())
  PRESCCODE  String @default("") @db.VarChar(10)
  MCODE      String @default("") @db.VarChar(10)
  MATERIAL   String @default("") @db.VarChar(40)
  UNITG      Float  @default(0) @db.Float
  APPDATE    String @default("") @db.VarChar(10)
  ENDDATE    String @default("") @db.VarChar(10)

  @@index([APPDATE], map: "APPDATE")
  @@index([ENDDATE], map: "ENDDATE")
  @@index([MATERIAL], map: "MATERIAL")
  @@index([PRESCCODE], map: "PRESCCODE")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TPRESCRIPTIONMASTER {
  PRESCID     Int    @id @default(autoincrement())
  PRESCTYPE   Int    @default(0) @db.TinyInt
  DTYPE       Int    @default(0) @db.TinyInt
  DISEASENAME String @default("") @db.VarChar(50)
  PRESCCODE   String @default("") @db.VarChar(10)
  PRESCNAME   String @default("") @db.VarChar(50)
  UNITPRICE   Int    @default(0)
  PRICE       Int    @default(0)
  APPDATE     String @default("") @db.VarChar(8)
  ENDDATE     String @default("") @db.VarChar(8)
  INSTYPE     String @default("") @db.VarChar(10)

  @@index([APPDATE], map: "APPDATE")
  @@index([DISEASENAME], map: "DISNAME")
  @@index([ENDDATE], map: "ENDDATE")
  @@index([PRESCCODE], map: "PRESCCODE")
  @@index([PRESCNAME], map: "PRESCNAME")
}

model TRAREDISEASE {
  RDISID    Int     @id @default(autoincrement())
  KCDCODE   String? @db.VarChar(100)
  RDISCODE  String? @db.Char(6)
  DISNAME   String? @db.VarChar(200)
  VCODE     String? @db.VarChar(12)
  CODERANGE String? @db.Text
  DISTYPE   String? @db.VarChar(2)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TSAMPLECODE {
  SPECIMENCODE String? @default("") @db.Char(4)
  SPECIMENNAME String? @default("") @db.VarChar(50)
  CONSIGNCOMID String? @default("") @db.VarChar(10)

  @@ignore
}

model TSPCODE {
  CID         Int    @id @default(autoincrement())
  SPCODE      String @default("0") @db.VarChar(10)
  SPTITLE     String @default("") @db.VarChar(64)
  SPFORMAT    String @default("") @db.VarChar(100)
  DESCRIPTION String @db.Text
  INSTYPE     Int?   @default(1) @db.TinyInt
  SPTYPE      Int?   @default(1) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TUNDISCLOSED {
  ID          Int     @id @default(autoincrement())
  DISEASETYPE String? @db.VarChar(20)
  KCDCODE     String? @db.VarChar(10)
  KOREAN      String? @db.VarChar(200)
  ENGLISH     String? @db.VarChar(200)
  VCODE       String? @db.VarChar(12)
}

model TVCODE {
  VCDID   BigInt  @id @default(autoincrement())
  VCODE   String? @db.VarChar(10)
  KORNAME String? @db.VarChar(100)
  ENGNAME String? @db.VarChar(200)
}

model mtxcInfo {
  id          Int    @id @default(autoincrement())
  ip          String @default("") @db.VarChar(50)
  port        String @default("") @db.VarChar(20)
  description String @default("") @db.VarChar(20)
  updatedAt   String @default("") @db.VarChar(30)

  @@unique([ip, port], map: "unique_ip_port")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model TKCD9 {
  KCDID       BigInt  @id @default(autoincrement())
  KCDCODE     String? @db.VarChar(10)
  KCDNAME_KR  String? @db.Text
  KCDNAME_EN  String? @db.Text
  INFECT      String? @db.VarChar(10)
  GENDER      String? @db.VarChar(2)
  AGE1        Int?    @db.SmallInt
  AGE2        Int?    @db.SmallInt
  INCOMLETE   Int?    @default(0) @db.TinyInt
  MT028       Float?  @default(0)
  DTYPE       String? @db.VarChar(20)
  MAINDISEASE String? @default("") @db.VarChar(2)
  CHUNACHECK  Int?    @default(0) @db.TinyInt
}

enum TKCD7_INCOMLETE {
  Y
  N
}
